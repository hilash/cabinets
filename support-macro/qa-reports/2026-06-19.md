# Weekly QA Report — 2026-06-19

**Auditor:** Macro QA Reviewer
**Rotation planned:** 7 active macros (oldest last-QA first) + 3 drafts awaiting first-time QA = 10 macros
**Status:** AUDIT BLOCKED — second consecutive week (see `/qa-reports/2026-06-12.md`)

## Result: 0 of 10 macros auditable

| Metric | Count |
|---|---|
| Macros audited | 0 |
| Pass | 0 |
| Pass with notes | 0 |
| Needs update | 0 |
| Broken links found | 0 (link check not executable) |
| Retirement candidates | 0 confirmed |

Nothing requested in last week's Writer task (`e4947db8`) has materialized. The blocking conditions are unchanged from 2026-06-12:

1. `/macros/` holds no active-macro body files (`/macros/<slug>.md`).
2. `/macros/drafts/` is still empty — the 3 drafts gating publishing have no files to QA.
3. `/data/release-notes.md` is still missing — accuracy check (a) has no source of truth.
4. `/article-registry.md` is still missing — link check (b) cannot verify article slugs.
5. `/clusters/` empty and `/data/cluster-summary.csv` missing — scope check (d) cannot verify cluster linkage.

`macro-library/data.json` was re-confirmed to be a **metadata-only** mirror (slugs, status, QA stamps) — it contains no macro bodies, so it cannot substitute for the missing files.

## Top 3 issues found

1. **Entire active library is now QA-overdue.** With no audit possible for a second week, every active macro has crossed the 4-week freshness threshold. Days since last QA as of today:
   - webhook-delivery-failure — 2026-05-02 → **48 days**
   - sso-okta-walkthrough — 2026-05-09 → 41 days
   - refund-request-standard — 2026-05-09 → 41 days
   - billing-invoice-explanation — 2026-05-16 → 34 days
   - password-reset-manual — 2026-05-16 → 34 days
   - api-rate-limit-429 — 2026-05-23 → 27 days (already flagged *Needs update*)
   - workspace-roles-overview — 2026-05-23 → 27 days (already flagged *Needs update*)

   This is no longer a single overdue macro — it is full-library staleness. The two *Needs update* flags have sat unaddressed for a month.

2. **Writer task unanswered for a full week.** The priority-1 task to materialize macro bodies and reference data (`e4947db8`, filed 2026-06-12) is still `pending`. No `macro-writer` heartbeat or job activity is visible in `.agents/.conversations/` for either week, which suggests the Writer agent may not be running — i.e. this is unlikely to self-resolve without human intervention. Escalated this week (see below).

3. **Scheduled freshness-QA job cannot self-complete when blocked.** Today's 13:00 UTC run (`2026-06-19T13-00-01-097Z`) started, hit the same missing-files discovery, and stalled — status still `running`, zero artifacts, no report produced. The 2026-06-12 run had the same problem. The automated gate is therefore non-functional in the blocked state; the weekly report exists only because the heartbeat wrote it. Process risk worth a human look independent of the macro-body gap.

## Secondary findings (carried forward, still unresolved)

- **Usage data stale.** `/data/macro-usage.csv` still ends at week 2026-W21 (now ~4 weeks old) and still has no draft-macro rows. Retirement and scope-drift calls remain un-makeable on this data.
- **index.md vs. registry conflict.** Last week's report flagged that `index.md` shows drafts `csv-export-row-limit` (24 uses/7d) and `ios-login-loop` (18 uses/7d) as if live, while the registry marks both Draft / 0 uses / Awaiting QA. If unreviewed drafts are actually deployed, the QA gate is being bypassed. Still unconfirmed and unresolved.

## Retirement candidates

None confirmed — retirement requires current usage + cluster volume, both still missing. **Watch item:** all 3 drafts (`csv-export-row-limit`, `ios-login-loop`, `billing-proration-explain`) have sat in *Awaiting QA* with 0 recorded usage since ≥2026-05-23. If bodies are still absent next week (week 3), recommend a deliberate decision on whether those clusters still warrant macros at all rather than leaving permanently un-actionable drafts on the books.

## Actions taken this week

- Re-ran the four-check verification against the filesystem — confirmed the block is unchanged, not a transient.
- **Escalated** Writer task `e4947db8` on the `support-macros` channel (added a 2026-06-19 escalation note flagging two consecutive blocked weeks; set `escalated: true`; priority already at max 1). This task is itself the inter-agent message to `macro-writer` (`toAgent: macro-writer`, `channel: support-macros`) — there is no separate chat-post mechanism. The stalled scheduled-job concern is captured in issue #3 above.
- Re-stamped all 10 per-macro results in `/macros/qa/<slug>-qa.md` with a 2026-06-19 re-check note (each still "Blocked"; QA date advanced on the result file only, not in the registry).
- Updated `/data/broken-links.md` to record that the 2026-06-19 link check could not execute.
- Updated the `/macro-registry.md` footer warning to reflect the block now spans 2026-06-12 → 2026-06-19 (footer status annotation only — no per-macro QA stamp or rotation-queue change, since nothing was audited).

QA will run the full rotation the moment macro bodies + reference data land — drafts first (they gate publishing), then active macros in last-QA-date order starting with webhook-delivery-failure.
