# Weekly QA Report — 2026-06-12

**Auditor:** Macro QA Reviewer
**Rotation planned:** 7 active macros (oldest last-QA first) + 3 drafts awaiting first-time QA = 10 macros

## Result: AUDIT BLOCKED — 0 of 10 macros auditable

| Metric | Count |
|---|---|
| Macros audited | 0 |
| Pass | 0 |
| Pass with notes | 0 |
| Needs update | 0 |
| Broken links found | 0 (check not executable) |
| Retirement candidates | 0 confirmed (see note on drafts below) |

## Why the audit is blocked

The registry (`/macro-registry.md`) lists 10 macros, but **no macro body files exist anywhere in the library**:

1. `/macros/` is empty — none of the 7 active macros have a body file at `/macros/<slug>.md`.
2. `/macros/drafts/` is empty — the 3 drafts marked "Awaiting QA" (`csv-export-row-limit`, `ios-login-loop`, `billing-proration-explain`) have no draft files to QA.
3. `/data/release-notes.md` does not exist — the accuracy check (a) has no source of truth for product changes.
4. `/article-registry.md` does not exist — the link check (b) cannot verify article slugs.
5. `/clusters/` is empty and `/data/cluster-summary.csv` does not exist — the scope check (d) cannot verify cluster linkage; per job rules, every macro's cluster is technically unverifiable.

The scheduled freshness-QA job ran today at 13:00 and reached the same discovery (conversation `2026-06-12T13-00-01`); its artifacts are listed in the addendum below.

## Top 3 issues found (registry/process level — the only auditable surface)

1. **Registry–filesystem divergence.** `/macro-registry.md` claims 10 macros with QA stamps through 2026-05-23, but the library contains zero files. Every "Passed" stamp in the registry is unverifiable. Until bodies exist, the registry overstates library health.
2. **Stale registry footer.** The footer says "Next macro to QA: workspace-roles-overview (last QA: May 2)" — but the table shows workspace-roles-overview was QA'd 2026-05-23; the actual oldest is **webhook-delivery-failure (2026-05-02)**, now >4 weeks stale and overdue.
3. **Stale usage data.** `/data/macro-usage.csv` ends at week 2026-W21 (~3 weeks ago) and contains no draft-macro rows. Retirement and scope-drift decisions cannot be made on 3-week-old data.

## Retirement candidates

None confirmed — retirement requires current usage data and cluster volume, both missing. Flag for next week: all 3 drafts have sat in "Awaiting QA" since at least 2026-05-23 with 0 usage; if bodies are not produced by the Writer this coming week, recommend deciding whether those clusters still warrant macros.

## Requested from Macro Writer (task filed)

- Materialize body files for all 7 active macros at `/macros/<slug>.md` and the 3 drafts at `/macros/drafts/<slug>.md`, or correct the registry to reflect what actually exists.
- Provide `/data/release-notes.md` and `/article-registry.md` (or point QA at the canonical equivalents).
- Refresh `/data/macro-usage.csv` and fix the registry footer ("next to QA" line).

QA will re-run the full rotation as soon as macro bodies land — drafts first (they gate publishing), then active macros in last-QA-date order starting with webhook-delivery-failure.

---

## Addendum — scheduled job run (2026-06-12, conversation `2026-06-12T13-00-01`)

The scheduled freshness-QA job completed the deliverables this report's first pass
could not produce:

- **Per-macro QA results** written to `/macros/qa/<slug>-qa.md` for all 10 selected
  macros (7 active + 3 drafts). Every result is **Blocked — macro body file missing**,
  each with the exact path the Writer must populate. No `qa-status` or `last-qa-date`
  was stamped in the registry — nothing was audited, so the rotation queue is unchanged.
- **Registry footer corrected** in `/macro-registry.md`: next-to-QA now points to
  webhook-delivery-failure (2026-05-02); counts fixed from "6 Active · 1 Needs update"
  to the table's actual **7 Active · 2 Needs update** (api-rate-limit-429 and
  workspace-roles-overview both carry the flag) — a fourth registry inconsistency
  beyond the three listed above. A blocked-pass warning was added to the footer.
- `/data/broken-links.md` confirmed initialised (no checkable links this week).

Additional finding for the Writer: `index.md` shows the drafts csv-export-row-limit
(24 uses/7d) and ios-login-loop (18 uses/7d) as if in live use. If unreviewed drafts
are actually deployed in Zendesk/Intercom, that bypasses the QA gate — please confirm
and correct whichever record is wrong.
